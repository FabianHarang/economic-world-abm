# HPC Runs

This folder will contain university/HPC cluster scripts, such as Slurm templates, after the local engine and benchmark protocol are stable.

HPC jobs must be reproducible from:

- scenario config;
- seed policy;
- code commit hash;
- environment file or container;
- output metadata.

No cluster-specific secrets or private credentials belong in git.

