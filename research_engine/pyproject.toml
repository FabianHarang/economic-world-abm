[project]
name = "world-abm-research-engine"
version = "0.7.0"
description = "Offline research-scale engine for the Economic World ABM"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["world_abm"]

[tool.pytest.ini_options]
testpaths = ["tests"]
